// DFS implementation, as seen in CLRS
/*eslint-env node*/
let dfs = () => {
    let dfs_visit = function () {
        
    };
    
    return {
        // graph: graph as adjacency list
        perform: function (graph) {
            
        }
    };
} 